# ============================================================================
# 基于 Orbbec 3D 视觉与姿态识别的办公久坐放松 AI 视觉对话助手
# ============================================================================
# 当前版本：动作模仿挑战游戏原型（PySide6 GUI）
# 后续将改造为办公久坐放松助手。
# ============================================================================

# ---------------------------------------------------------------------------
# 核心依赖（必需）
# ---------------------------------------------------------------------------
PySide6>=6.5.0              # Qt GUI 框架（主界面 + 控件 + 信号槽）
opencv-python>=4.8.0        # OpenCV 图像处理（cv2）
numpy>=1.24.0               # 数值计算
onnxruntime>=1.16.0         # ONNX 模型推理引擎（YOLO 检测）
mediapipe>=0.10.0           # Google MediaPipe 姿态估计（33 关键点）
pillow>=9.0.0               # PIL 中文文字渲染（微软雅黑）
pyorbbecsdk2>=2.0.0         # Orbbec 3D 深度相机 SDK（安装后 import pyorbbecsdk）
pygame>=2.5.0               # 背景音乐播放（鸽子舞.mp3）

# ---------------------------------------------------------------------------
# GPU 加速（推荐安装，可大幅提升帧率）
# ---------------------------------------------------------------------------
# Windows: onnxruntime-directml
# Linux:   onnxruntime-gpu (需 CUDA)
# macOS:   onnxruntime 已内置 CoreML 加速，无需额外安装
# onnxruntime-directml

# ---------------------------------------------------------------------------
# 可选依赖
# ---------------------------------------------------------------------------
# open3d>=0.18.0             # 3D 骨架可视化窗口（游戏中按 J 开关）
                             # 不安装不影响游戏核心功能

# ---------------------------------------------------------------------------
# 注意事项
# ---------------------------------------------------------------------------
# 1. mediapipe 0.10.x 要求 protobuf < 5，如遇到版本冲突请执行:
#    pip install protobuf==4.25.9
#
# 2. pyorbbecsdk2 是 Orbbec SDK v2 的 Python 包名，安装后使用 import pyorbbecsdk。
#    请参考 Orbbec 官方文档安装: https://github.com/orbbec/pyorbbecsdk
#
# 3. 如果安装 onnxruntime-directml 后推理仍用 CPU，请确认:
#    - Windows 已安装 DirectX 12
#    - GPU 驱动已更新到最新版本
#
# 4. 以下库是 Python 标准库，不需要安装:
#    os, sys, time, json, threading, collections, datetime,
#    argparse, random, winsound, traceback
# ============================================================================
